# xrpl4j Demo

## Run the app

Update `MptService` in the `src` directory accordingly (the issuer and holder seed for example, the maximum amount to mint...).

Run:

```java
./mvnw compile exec:java -Dexec.mainClass=com.xrpl4j.xrpl4j_demo.MptService
```
